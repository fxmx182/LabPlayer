<?xml version="1.0" encoding="utf-8"?>
<!-- O painel de ferramentas.

     Dezesseis itens em lista de texto é uma coluna que não cabe na tela e que
     se lê linha a linha. Em grade de quatro, tudo aparece de uma vez e o dedo
     vai direto — e os grupos dizem onde olhar antes de o olho percorrer tudo. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:background="@drawable/bg_tools_sheet"
    android:paddingTop="12dp"
    android:paddingBottom="26dp">

    <View
        android:layout_width="40dp"
        android:layout_height="4dp"
        android:layout_gravity="center_horizontal"
        android:layout_marginBottom="4dp"
        android:background="@drawable/bg_pega" />

    <ScrollView
        android:id="@+id/tools_scroll"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:scrollbars="none"
        android:paddingHorizontal="12dp"
        android:clipToPadding="false">

        <LinearLayout
            android:id="@+id/tools_container"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical" />
    </ScrollView>
</LinearLayout>
